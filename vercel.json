{
    "rewrites": [
        {
            "source": "/:path*",
            "destination": "/index.html"
        }
    ],
    "trailingSlash": false,
    "buildCommand": "npm install -g vite && vite build",
    "outputDirectory": "dist"
}
