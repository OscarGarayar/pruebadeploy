{
    "rewrites": [
        {
            "source": "/:path*",
            "destination": "/"
        }
    ],
    "trailingSlash": false,
    "buildCommand": "npm install -g vite && vite build",
    "outputDirectory": "dist"
}
